--- a/Makefile	2012-11-14 12:26:58.000000000 +0400
+++ b/Makefile	2012-12-05 12:03:30.261339143 +0400
@@ -2,7 +2,7 @@
 #LUA     = lua-5.2.0
 STATIC  = false
 
-CFLAGS  = -fno-exceptions -fno-rtti -O2 -I$(LUA) -L$(LUA)
+CFLAGS  += -fno-exceptions -fno-rtti
 SRC     = main.cpp soap.cpp mem.cpp mcast.cpp luaxlib.cpp luaxcore.cpp luajson.cpp luajson_parser.cpp
 STAGING_DIR = /home/shocker/staff/openwrt/staging_dir
 LUAMYCFLAGS = -DLUA_USE_LINUX
@@ -12,6 +12,8 @@
 LUAMYCFLAGS+=-static
 endif
 
+all: embedded
+
 x86:
 	make -C $(LUA) CC=gcc a
 	gcc -O2 -c -o md5.o md5c.c
@@ -53,10 +55,8 @@
 embedded:
 	export PATH
 	export STAGING_DIR
-	make -C $(LUA) CC=$(SDK)/gcc a MYCFLAGS='$(LUAMYCFLAGS)'
-	$(SDK)/gcc -O2 -c -o md5.o md5c.c
-	$(SDK)/gcc $(CFLAGS) -DWITH_URANDOM -o xupnpd-$(TARGET) $(SRC) md5.o -llua -lm -ldl
-	$(SDK)/strip xupnpd-$(TARGET)
+	$(CC) -O2 -c -o md5.o md5c.c
+	$(CC) $(CFLAGS) -DWITH_URANDOM $(LDFLAGS) -o xupnpd $(SRC) md5.o -llua -lm -ldl
 
 clean:
 	make -C $(LUA) clean
